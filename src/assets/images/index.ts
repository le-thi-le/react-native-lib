import type { ImageSource } from './type';
export { RemoteImage } from './type';
export type { ImageSource } from './type';

export const imageExample: ImageSource = {
  imageSource: require('./source/imageName.png'),
};
